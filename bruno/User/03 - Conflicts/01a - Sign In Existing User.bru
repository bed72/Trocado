meta {
  name: Conflict - Sign In Existing User
  type: http
  seq: 2
  tags: [
    user
    conflict
  ]
}

post {
  url: {{baseUrl}}/api/authentication/sign-in
  body: json
  auth: none
}

headers {
  Content-Type: application/vnd.api+json
}

body:json {
  {
    "data": {
      "type": "access-tokens",
      "attributes": {
        "email": "{{conflictUserEmail}}",
        "password": "{{conflictUserPassword}}"
      }
    }
  }
}

script:post-response {
  if (res.getStatus() === 200) {
    bru.setVar("authenticationToken", res.getBody().data.attributes.token);
  }
}

tests {
  test("authenticates the existing user", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
