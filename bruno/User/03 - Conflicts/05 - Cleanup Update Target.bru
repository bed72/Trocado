meta {
  name: Conflict - Cleanup Update Target
  type: http
  seq: 5
  tags: [
    user
    conflict
    cleanup
  ]
}

delete {
  url: {{baseUrl}}/api/users/{{conflictTargetUserId}}
  body: none
  auth: none
}


tests {
  test("removes the update target", function () {
    expect(res.getStatus()).to.equal(204);
  });
}
