meta {
  name: Update - Invalid Email
  type: http
  seq: 5
  tags: [
    user
    validation
  ]
}

patch {
  url: {{baseUrl}}/api/users/{{validationUserId}}
  body: json
  auth: inherit
}

headers {
  Content-Type: application/vnd.api+json
}

body:json {
  {
    "data": {
      "type": "users",
      "id": "{{validationUserId}}",
      "attributes": {
        "email": "invalid"
      }
    }
  }
}

tests {
  test("returns HTTP 422", function () {
    expect(res.getStatus()).to.equal(422);
  });

  test("points to the invalid email", function () {
    expect(res.getBody().errors[0].source.pointer).to.equal("/data/attributes/email");
  });
}
