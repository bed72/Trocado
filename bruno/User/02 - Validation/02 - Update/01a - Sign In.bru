meta {
  name: Update Validation - Sign In
  type: http
  seq: 2
  tags: [
    user
    validation
  ]
}

post {
  url: {{baseUrl}}/api/authentication/sign-in
  body: json
  auth: none
}

headers {
  Content-Type: application/vnd.api+json
}

body:json {
  {
    "data": {
      "type": "access-tokens",
      "attributes": {
        "email": "{{validationUserEmail}}",
        "password": "{{validationUserPassword}}"
      }
    }
  }
}

script:post-response {
  if (res.getStatus() === 200) {
    bru.setVar("authenticationToken", res.getBody().data.attributes.token);
  }
}

tests {
  test("authenticates the validation target", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
