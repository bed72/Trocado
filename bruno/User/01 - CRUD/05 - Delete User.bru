meta {
  name: Delete User
  type: http
  seq: 7
  tags: [
    user
    crud
  ]
}

delete {
  url: {{baseUrl}}/api/users/{{userId}}
  body: none
  auth: inherit
}


tests {
  test("returns HTTP 204", function () {
    expect(res.getStatus()).to.equal(204);
  });
}
