meta {
  name: Delete Budget
  type: http
  seq: 5
  tags: [
    smoke
    budget
    crud
  ]
}

delete {
  url: {{baseUrl}}/api/budgets/{{budgetId}}
  body: none
  auth: none
}

headers {
  Accept: application/vnd.api+json
}

tests {
  test("returns HTTP 204", function () {
    expect(res.getStatus()).to.equal(204);
  });
}
