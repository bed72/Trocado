meta {
  name: Create - Invalid Date Range
  type: http
  seq: 4
  tags: [
    budget
    validation
  ]
}

post {
  url: {{baseUrl}}/api/budgets
  body: json
  auth: inherit
}

headers {
  Content-Type: application/vnd.api+json
}

body:json {
  {
    "data": {
      "type": "budgets",
      "attributes": {
        "amount": 12500,
        "start_date": "2026-09-30",
        "end_date": "2026-09-01"
      }
    }
  }
}

tests {
  test("returns HTTP 422", function () {
    expect(res.getStatus()).to.equal(422);
  });

  test("points to the end date", function () {
    expect(res.getBody().errors[0].source.pointer).to.equal("/data/attributes/end_date");
  });
}
