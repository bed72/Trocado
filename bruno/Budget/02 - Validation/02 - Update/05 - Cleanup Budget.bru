meta {
  name: Update Validation - Cleanup Budget
  type: http
  seq: 5
  tags: [
    budget
    validation
  ]
}

delete {
  url: {{baseUrl}}/api/budgets/{{validationBudgetId}}
  body: none
  auth: inherit
}


tests {
  test("removes the validation target", function () {
    expect(res.getStatus()).to.equal(204);
  });
}
