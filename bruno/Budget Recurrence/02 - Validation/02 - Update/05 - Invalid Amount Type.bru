meta {
  name: Update - Invalid Amount Type
  type: http
  seq: 5
  tags: [
    budget-recurrence
    validation
  ]
}

patch {
  url: {{baseUrl}}/api/budget-recurrences/{{validationRecurrenceId}}
  body: json
  auth: none
}

headers {
  Content-Type: application/vnd.api+json
}

body:json {
  {
    "data": {
      "type": "budget-recurrences",
      "id": "{{validationRecurrenceId}}",
      "attributes": {
        "amount": "80000"
      }
    }
  }
}

tests {
  test("returns HTTP 422", function () {
    expect(res.getStatus()).to.equal(422);
  });

  test("points to the amount attribute", function () {
    expect(res.getBody().errors[0].source.pointer).to.equal("/data/attributes/amount");
  });
}
