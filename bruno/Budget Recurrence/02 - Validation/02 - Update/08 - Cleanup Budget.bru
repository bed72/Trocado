meta {
  name: Update Validation - Cleanup Budget
  type: http
  seq: 8
  tags: [
    budget-recurrence
    validation
    cleanup
  ]
}

delete {
  url: {{baseUrl}}/api/budgets/{{validationRecurringBudgetId}}
  body: none
  auth: none
}


tests {
  test("removes the validation budget", function () {
    expect(res.getStatus()).to.equal(204);
  });
}
