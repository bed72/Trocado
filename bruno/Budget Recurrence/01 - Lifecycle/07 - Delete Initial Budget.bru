meta {
  name: Delete Initial Budget
  type: http
  seq: 7
  tags: [
    budget-recurrence
    lifecycle
    cleanup
  ]
}

delete {
  url: {{baseUrl}}/api/budgets/{{recurringBudgetId}}
  body: none
  auth: none
}

headers {
  Accept: application/vnd.api+json
}

tests {
  test("returns HTTP 204", function () {
    expect(res.getStatus()).to.equal(204);
  });
}
